tspsrc = tsp.c io.c renormalization.c tsp.h io.h renormalization.h distance.h distance.c

AM_CPPFLAGS = -Wall -pedantic -g $(GSL_CFLAGS)  -w  -std=c99
AM_LDFLAGS = $(GSL_LIBS)

noinst_PROGRAMS = tsp

tsp_SOURCES = $(tspsrc)
